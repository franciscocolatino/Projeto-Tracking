<!DOCTYPE html>
<html>
  <head>
    <title>Lista de tarefas</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags shim: false  %>
  </head>

  <body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
      <div class="container-fluid">
        <%= link_to "Tarefas", tasks_path, class: "btn btn-info text-white fs-8" %>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
          <span class="navbar-toggler-icon"></span>
        </button>
        <div class="collapse navbar-collapse" id="navbarSupportedContent">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Dashboard", dashboard_path, class: "btn btn-danger text-white fs-8 mx-2" %>
            </li>
          </ul>
        </div>
        <div class="d-flex">
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Criar nova tarefa", new_task_path, class: "btn btn-primary" %>
            </li>
          </ul>
          <ul class="navbar-nav me-auto mb-2 mb-lg-0">
            <li class="nav-item">
              <%= link_to "Criar nova coluna", new_kanban_column_path, class: "btn btn-success mx-2" %>
            </li>
          </ul>
        </div>

      </div>
    </nav>


    <%= yield %>
  </body>
</html>
