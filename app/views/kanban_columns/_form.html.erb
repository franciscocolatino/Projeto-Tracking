<%= form_with(model: kanban_column) do |form| %>
  <% if kanban_column.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(kanban_column.errors.count, "error") %> prohibited this kanban_column from being saved:</h2>

      <ul>
        <% kanban_column.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :name, style: "display: block" %>
    <%= form.text_field :name %>
  </div>

  <div>
    <%= form.label :position, style: "display: block" %>
    <%= form.number_field :position %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
